import Foundation

// MARK: - Reconciliation Service (Stub — Build 18)

/// Merges new RawExtractions into existing CanonicalEntities.
/// Handles deduplication, alias detection, and belief score updates.
///
/// This is a stub. Full implementation in Build 18 (Section 3).
enum ReconciliationService {

    /// Reconcile new extractions against existing canonical entities.
    /// Currently a no-op — will be implemented in Build 18.
    static func reconcile(extractions: [RawExtraction]) {
        // Stub — Build 18 implementation
    }
}
